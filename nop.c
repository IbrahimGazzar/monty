#include "monty.h"

/**
 * nop - nop
 * @stack: stack to be nopped
 * @l_num: number of the line in the file
 *
 * Return: no return value
 */

void nop(stack_t **stack, unsigned int l_num)
{
	(void) stack;
	(void) l_num;
}
